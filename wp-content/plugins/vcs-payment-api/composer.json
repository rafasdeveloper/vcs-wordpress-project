{
    "name": "vcs/payment-api",
    "description": "VCS Payment API Plugin for WordPress",
    "type": "wordpress-plugin",
    "require": {
        "php": ">=7.4",
        "paypal/paypal-server-sdk": "^0.5.0"
    },
    "autoload": {
        "psr-4": {
            "VCS\\PaymentAPI\\": "src/"
        }
    },
    "scripts": {
        "post-install-cmd": [
            "@php -r \"if (!file_exists('vendor/autoload.php')) { echo 'Please run composer install to install dependencies.'; exit(1); }\""
        ]
    }
}